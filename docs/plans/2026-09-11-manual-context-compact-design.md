# 未完成 · 2026-09-11 手动压缩上下文

用户确认：输入框敲 `/compact`（`/压缩` 也认）；窗口宽裕也允许；命令词要和普通字区分，并给出命令提示。

## 要改什么

- 输入框：开头 `/` 出提示条；对得上的命令词高亮；Tab 补全、回车执行，不进对话气泡
- Agent：空闲时走现有交接，跳过「窗口快满」门槛；正在跑则拒绝
- 接线：桌面输入框 → 后端交接；欢迎页空对话直接说没什么可压

## 不做

- 完整斜杠菜单体系、点用量数字、IM、清空对话
