# 已完成

2026-09-15 主动压缩上下文四档

用户确认：设置里增加「主动压缩上下文」，四档——较多、适中（默认）、较少、不主动压缩。选不同档，只给 Agent 不同提示词。

## 要改什么

- 共享一处枚举：`more | balanced | less | off`，默认 `balanced`
- 配置键 `proactiveCompact`，缺省或脏值回落到适中
- 设置 → AI：分段四选一；说明写清只调它有多主动，快满和人手动压不受影响
- 系统说明按档替换「上下文开销」一段；工具说明不再写「该不该压」的账，避免跟「不主动压缩 / 较少」打架
- 快满系统压、`/compact` 都不动

## 不做

- 不另做一套压缩
- 不按价目自动换档
- 不按用量动态改工具说明
