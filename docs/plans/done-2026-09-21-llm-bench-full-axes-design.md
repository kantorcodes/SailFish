# 已完成

2026-09-21 压测四根轴一次跑完

用户确认：全面完善。除上下文长短外，补上输出速度、真的在调工具、同时打几路；点开始打完四块，一张报告。

设计目标已写入 `electron/services/llm-bench/SPEC.md`。

## 要改什么

- 题升到 `sailfish-bench-v3`：短回、指定正文、读文件往返三套指令；各轴题头隔离，避免吃缓存
- 跑手按顺序：长度扫 → 输出 → 工具两段 → 三路并发；停则中止所有在飞的请求
- 流式请求可指定「必须调工具」（只给压测用）
- 压测窗四块表

## 不做

- 不评质量、不真执行工具、不扫 QPS、不自动换模型、不悄悄重试

## 任务拆解

- [x] SPEC
- [x] 题 + 单测
- [x] 跑手四段 + 单测
- [x] 压测窗四块表
- [x] CLI 回归
- [x] 2026-09-21 补：跑完给压测分，跳过/失败按零
